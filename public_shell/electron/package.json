{
  "name": "nexusschoolos",
  "productName": "NexusSchoolOS",
  "version": "1.0.0",
  "description": "High-End School Management System - Desktop Shell",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "pack": "npx @electron/packager . NexusSchoolOS --platform=darwin --arch=arm64,x64 --icon=assets/icon.icns --overwrite --out=dist"
  },
  "dependencies": {
    "@nexus/engine": "file:../../private_engine",
    "address": "^1.2.2",
    "better-sqlite3": "^11.10.0",
    "csv-parser": "^3.2.0",
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "fs": "^0.0.1-security",
    "handlebars": "^4.7.9",
    "qrcode": "^1.5.3"
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "electron-rebuild": "^3.2.9"
  }
}
